{
  "name": "gulp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^4.0.1",
    "gulp-watch": "^5.0.0"
  },
  "scripts": {
    "dev": "gulp clean && gulp dev",
    "build": "gulp clean && gulp"
  }
}
